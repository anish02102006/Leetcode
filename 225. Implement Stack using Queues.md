Chalo bhai 😄
**LeetCode 225 – Implement Stack using Queues** ko **clear + interview-ready** tareeke se samajhte hain.
Main **2 approaches** dikhaunga (both valid), **Java + Hinglish** me.

---

## 🔴 Problem Samajh Lo

Tumhe **Stack (LIFO)** implement karna hai
lekin sirf **Queue ke operations** use kar sakte ho.

### Stack Operations

* `push(x)`
* `pop()`
* `top()`
* `empty()`

### Queue Allowed Ops

* `add / offer`
* `remove / poll`
* `peek`
* `isEmpty`
* `size`

---

# 🟠 Approach 1: Two Queues (Push Costly)

### 🧠 Idea

* Hamesha **naya element ko front** me lao
* Taaki pop/top **O(1)** me ho jaaye

---

### 🔄 Push Logic

1. New element → `q2`
2. `q1` ke saare elements → `q2`
3. `q1` aur `q2` swap

---

### ✅ Java Code (Two Queues)

```java
import java.util.*;

class MyStack {

    Queue<Integer> q1 = new LinkedList<>();
    Queue<Integer> q2 = new LinkedList<>();

    public void push(int x) {
        q2.add(x);

        while (!q1.isEmpty()) {
            q2.add(q1.remove());
        }

        Queue<Integer> temp = q1;
        q1 = q2;
        q2 = temp;
    }

    public int pop() {
        return q1.remove();
    }

    public int top() {
        return q1.peek();
    }

    public boolean empty() {
        return q1.isEmpty();
    }
}
```

---

### ⏱ Time Complexity

| Operation | Time |
| --------- | ---- |
| push      | O(n) |
| pop       | O(1) |
| top       | O(1) |
| empty     | O(1) |

### 📦 Space: `O(n)`

---

# 🟢 Approach 2: Single Queue (Most Popular 🔥)

### 🧠 Smart Trick

* Ek hi queue rakho
* Push ke baad queue ko **rotate** kar do
  taaki last inserted element front pe aa jaaye

---

### 🔄 Push Logic

```
push(x)
queue = [old elements..., x]

Rotate:
(size-1) times:
    queue.add(queue.remove())
```

---

### ✅ Java Code (Single Queue – Best)

```java
import java.util.*;

class MyStack {

    Queue<Integer> q = new LinkedList<>();

    public void push(int x) {
        q.add(x);
        int size = q.size();

        for (int i = 1; i < size; i++) {
            q.add(q.remove());
        }
    }

    public int pop() {
        return q.remove();
    }

    public int top() {
        return q.peek();
    }

    public boolean empty() {
        return q.isEmpty();
    }
}
```

---

### ⏱ Time Complexity

| Operation | Time |
| --------- | ---- |
| push      | O(n) |
| pop       | O(1) |
| top       | O(1) |
| empty     | O(1) |

### 📦 Space: `O(n)`

---

## 🧠 Dry Run (Single Queue)

```
push(1) → [1]
push(2) → add → [1,2]
rotate → [2,1]
push(3) → add → [2,1,3]
rotate → [3,2,1]
```

👉 Stack top always front pe 🔥

---

## 🎯 Interview Tip

Agar interviewer bole:

> “Best approach?”

Tum bolo:

> “Single queue using rotation, push O(n), pop O(1)”

---

## 🧩 Similar Questions

* 232. Implement Queue using Stacks
* 155. Min Stack
* 20. Valid Parentheses

---

Agar chaaho to mai:

* **Queue using Stack**
* **Dry run animation**
* **Interview explanation script**
* **Tricky follow-up questions**

sab karwa dunga 😎
Bas bolo bhai 👊
