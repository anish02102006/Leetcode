Chalo bhai 😄
**LeetCode 56 – Merge Intervals** ko **Brute → Better → Optimal** style me **Java + Hinglish** me bilkul clear kar dete hain 🔥

---

## 🔴 Problem Samajh Lo

Tumhe intervals diye gaye hain:

```
intervals[i] = [start, end]
```

Agar intervals **overlap** karte hain → unko **merge** karna hai.

---

### Example

```
Input:  [[1,3],[2,6],[8,10],[15,18]]
Output: [[1,6],[8,10],[15,18]]
```

---

## 🔑 Overlap Rule

Do intervals overlap karte hain agar:

```
current.start <= previous.end
```

---

# 🟠 Approach 1: Brute Force ❌

### 🧠 Idea

* Har interval ko baaki sab se compare karo
* Overlaps merge karte jao

### ❌ Problem

* Complex
* O(n²)
* Interview me weak

➡️ **Skip**

---

# 🟡 Approach 2: Sorting + Linear Scan (Standard)

### 🔥 Idea

1. Intervals ko **start time ke according sort** karo
2. Ek current interval rakho
3. Agar overlap ho → merge
4. Nahi ho → result me daal do

---

### Step-by-step

```
[[1,3],[2,6],[8,10],[15,18]]

Sort → [[1,3],[2,6],[8,10],[15,18]]

[1,3] & [2,6] → overlap → [1,6]
[1,6] & [8,10] → no overlap → push [1,6]
...
```

---

### ✅ Java Code (Optimal)

```java
import java.util.*;

class Solution {
    public int[][] merge(int[][] intervals) {

        Arrays.sort(intervals, (a, b) -> a[0] - b[0]);

        List<int[]> res = new ArrayList<>();

        int[] current = intervals[0];
        res.add(current);

        for (int i = 1; i < intervals.length; i++) {
            int[] next = intervals[i];

            if (next[0] <= current[1]) {
                // overlap
                current[1] = Math.max(current[1], next[1]);
            } else {
                current = next;
                res.add(current);
            }
        }

        return res.toArray(new int[res.size()][]);
    }
}
```

---

### ⏱ Time Complexity

* Sorting → `O(n log n)`
* Traversal → `O(n)`

### 📦 Space

* `O(n)` (result list)

🔥 **Most Optimal & Interview Favorite**

---

## 🧠 Edge Cases

* Already merged intervals
* Single interval
* Touching intervals like `[1,4]` & `[4,5]` → merged

---

## 🧠 Comparison Table

| Approach    | Time       | Space | Interview |
| ----------- | ---------- | ----- | --------- |
| Brute       | O(n²)      | O(1)  | ❌         |
| Sort + Scan | O(n log n) | O(n)  | 🔥        |

---

## 🎯 Interview One-Liner

> “Sir, intervals ko start ke basis pe sort karke ek linear scan me merge kar sakte hain.”

---

## 🧩 Related Questions

* Insert Interval
* Non-overlapping Intervals
* Meeting Rooms
* Minimum Arrows to Burst Balloons

---

Agar chaaho to mai:

* **Dry run animation**
* **Insert Interval explain**
* **Meeting Rooms ka logic**
* **All interval patterns**

sab cover kara dunga 😎
Bas bolo bhai 👊
